{
  "name": "studentport",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev -- --port 3002",
    "build": "cd client && npm run build",
    "preview": "cd client && npm run preview"
  }
}
